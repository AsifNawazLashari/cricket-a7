name: ashes_cricket
description: Professional Ashes Cricket Scoring App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.2
  web_socket_channel: ^2.4.0
  google_fonts: ^6.1.0
  provider: ^6.1.1
  lucide_icons: ^0.1.3 # For clean bottom nav icons

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
